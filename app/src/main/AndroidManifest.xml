<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.baiyyyhjl.slidingmenu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <!--<activity-->
        <!--android:name=".MainActivity"-->
        <!--android:label="@string/app_name" >-->
        <!--<intent-filter>-->
        <!--<action android:name="android.intent.action.MAIN" />-->

        <!--<category android:name="android.intent.category.LAUNCHER" />-->
        <!--</intent-filter>-->
        <!--</activity>-->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".example.AttachExample" />
        <activity android:name=".example.PropertiesActivity" />
        <activity android:name=".example.LeftAndRightActivity" />
        <activity android:name=".example.anim.CustomAnimation" />
        <activity android:name=".example.ViewPagerActivity" />
        <activity android:name=".example.FragmentChangeActivity" />
        <activity android:name=".example.ResponsiveUIActivity" />
        <activity android:name=".example.BirdActivity" />
    </application>

</manifest>
